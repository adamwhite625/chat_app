import { SigninForm } from "@/components/auth/signin-form";

const SignInPage = () => {
  return (
    <div className="flex min-h-svh w-screen items-center justify-center bg-muted">
      <SigninForm />
    </div>
  );
};

export default SignInPage;
